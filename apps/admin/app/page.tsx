/**
 * Admin dashboard root — /
 * Scaffold placeholder. Replace with full dashboard layout
 * once the admin task files are executed.
 */
export default function AdminDashboard() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-background-dark">
      <div className="text-center">
        <h1 className="font-display text-4xl text-text-high mb-2">
          Crib Community <span className="text-primary">Admin</span>
        </h1>
        <p className="text-text-medium">Dashboard scaffold — ready for build tasks.</p>
      </div>
    </main>
  )
}
